import React, { useState, useEffect } from 'react'; import { motion } from 'framer-motion'; import { Heart, Shield, Award, Users } from 'lucide-react'; export default function About() { const [counters, setCounters] = useState({ clientes: 0, entregas: 0, anos: 0, satisfacao: 0 }); const targetValues = { clientes: 15000, entregas: 50000, anos: 8, satisfacao: 98 }; useEffect(() => { const animateCounters = () => { const duration = 2000; const steps = 60; const increment = { clientes: targetValues.clientes / steps, entregas: targetValues.entregas / steps, anos: targetValues.anos / steps, satisfacao: targetValues.satisfacao / steps }; let currentStep = 0; const timer = setInterval(() => { currentStep++; setCounters({ clientes: Math.min(Math.floor(increment.clientes * currentStep), targetValues.clientes), entregas: Math.min(Math.floor(increment.entregas * currentStep), targetValues.entregas), anos: Math.min(Math.floor(increment.anos * currentStep), targetValues.anos), satisfacao: Math.min(Math.floor(increment.satisfacao * currentStep), targetValues.satisfacao) }); if (currentStep >= steps) { clearInterval(timer); } }, duration / steps); }; const observer = new IntersectionObserver( (entries) => { if (entries[0].isIntersecting) { animateCounters(); observer.disconnect(); } }, { threshold: 0.5 } ); const element = document.getElementById('about-stats'); if (element) observer.observe(element); return () => observer.disconnect(); }, []); const values = [ { icon: Heart, title: 'Paixão', description: 'Amor genuíno pela culinária brasileira e pela satisfação dos nossos clientes.' }, { icon: Shield, title: 'Qualidade', description: 'Compromisso inabalável com ingredientes frescos e preparo cuidadoso.' }, { icon: Award, title: 'Excelência', description: 'Busca constante pela perfeição em cada prato que servimos.' }, { icon: Users, title: 'Comunidade', description: 'Construindo relacionamentos duradouros com nossa família de clientes.' } ]; return ( <section id="about" className="py-20 bg-gradient-to-b from-gray-50 to-white"> <div className="container mx-auto px-4 lg:px-8"> <motion.div initial={{ opacity: 0, y: 50 }} whileInView={{ opacity: 1, y: 0 }} transition={{ duration: 0.8 }} viewport={{ once: true }} className="text-center mb-16" > <h2 className="text-4xl lg:text-5xl font-bold text-gray-900 mb-6"> Sobre a <span className="text-orange-500">BRASIL FOOOODS</span> </h2> <p className="text-xl text-gray-600 max-w-3xl mx-auto leading-relaxed"> Somos mais que um delivery de comida. Somos uma ponte que conecta você aos sabores autênticos do Brasil, levando tradição e inovação diretamente à sua mesa. </p> </motion.div> <div className="grid lg:grid-cols-2 gap-16 items-center mb-20"> <motion.div initial={{ opacity: 0, x: -50 }} whileInView={{ opacity: 1, x: 0 }} transition={{ duration: 0.8 }} viewport={{ once: true }} > <h3 className="text-3xl font-bold text-gray-900 mb-6">Nossa História</h3> <p className="text-gray-600 mb-6 leading-relaxed"> Nascemos do sonho de democratizar o acesso à boa comida brasileira. Desde 2016, temos o compromisso de oferecer pratos que carregam a alma e o tempero único do nosso país. </p> <p className="text-gray-600 mb-8 leading-relaxed"> Cada receita é cuidadosamente desenvolvida por chefs especializados na culinária regional, garantindo que cada mordida seja uma viagem pelos sabores do Brasil. </p> <motion.button whileHover={{ scale: 1.05 }} whileTap={{ scale: 0.95 }} className="bg-orange-500 text-white px-8 py-3 rounded-full font-semibold hover:bg-orange-600 transition-colors" > Conheça Nossa História </motion.button> </motion.div> <motion.div initial={{ opacity: 0, x: 50 }} whileInView={{ opacity: 1, x: 0 }} transition={{ duration: 0.8 }} viewport={{ once: true }} className="relative" > <div className="bg-gradient-to-br from-orange-500 to-red-500 rounded-2xl p-8 text-white"> <h4 className="text-2xl font-bold mb-4">Missão</h4> <p className="mb-6">Levar a autêntica culinária brasileira para todas as casas, conectando pessoas através dos sabores que nos unem.</p> <h4 className="text-2xl font-bold mb-4">Visão</h4> <p>Ser reconhecida como a principal referência em delivery de comida brasileira, expandindo nossa presença nacional.</p> </div> </motion.div> </div> <motion.div id="about-stats" initial={{ opacity: 0, y: 50 }} whileInView={{ opacity: 1, y: 0 }} transition={{ duration: 0.8 }} viewport={{ once: true }} className="grid grid-cols-2 lg:grid-cols-4 gap-8 mb-20" > {[ { number: counters.clientes.toLocaleString(), label: 'Clientes Satisfeitos', suffix: '+' }, { number: counters.entregas.toLocaleString(), label: 'Entregas Realizadas', suffix: '+' }, { number: counters.anos, label: 'Anos de Experiência', suffix: '' }, { number: counters.satisfacao, label: 'Satisfação dos Clientes', suffix: '%' } ].map((stat, index) => ( <motion.div key={index} whileHover={{ scale: 1.05 }} className="text-center bg-white rounded-lg p-6 shadow-lg border border-gray-100" > <div className="text-4xl font-bold text-orange-500 mb-2"> {stat.number}{stat.suffix} </div> <div className="text-gray-600 font-medium">{stat.label}</div> </motion.div> ))} </motion.div> <motion.div initial={{ opacity: 0, y: 50 }} whileInView={{ opacity: 1, y: 0 }} transition={{ duration: 0.8 }} viewport={{ once: true }} > <h3 className="text-3xl font-bold text-center text-gray-900 mb-12">Nossos Valores</h3> <div className="grid md:grid-cols-2 lg:grid-cols-4 gap-8"> {values.map((value, index) => ( <motion.div key={index} initial={{ opacity: 0, y: 30 }} whileInView={{ opacity: 1, y: 0 }} transition={{ duration: 0.6, delay: index * 0.1 }} viewport={{ once: true }} whileHover={{ y: -10 }} className="text-center bg-white rounded-lg p-8 shadow-lg border border-gray-100 hover:shadow-xl transition-all duration-300" > <div className="bg-orange-100 w-16 h-16 rounded-full flex items-center justify-center mx-auto mb-6"> <value.icon className="w-8 h-8 text-orange-500" /> </div> <h4 className="text-xl font-bold text-gray-900 mb-4">{value.title}</h4> <p className="text-gray-600 leading-relaxed">{value.description}</p> </motion.div> ))} </div> </motion.div> </div> </section> ); }